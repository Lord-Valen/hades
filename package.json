{
  "name": "hades-monorepo",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "build:clean": "tsc --build --clean"
  },
  "workspaces": [
    "packages/*"
  ]
}